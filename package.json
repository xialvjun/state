{
  "name": "@xialvjun/state",
  "version": "0.0.1",
  "description": "An EventEmitter which is stateful and has `setState` method which can debounce `change` event. Like Flux.",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/xialvjun/state.git"
  },
  "keywords": [
    "flux"
  ],
  "author": "xialvjun@live.com",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^2.8.1"
  },
  "dependencies": {
    "@nodeart/event_emitter": "^1.2.1"
  }
}
